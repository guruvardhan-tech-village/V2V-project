<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 🌍 Permissions for Maps and Location -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- 📞 Optional telephony feature for emergency calling -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_c2c_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_c2c_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.C2C_V2">
        
        <!-- 🗺 Google Maps API Key injected from local.properties via manifest placeholder -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
        
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
